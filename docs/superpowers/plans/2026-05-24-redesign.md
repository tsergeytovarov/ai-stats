# Liquid Glass Redesign — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Полностью переделать визуал поповера, виджетов и menu bar item под Apple Liquid Glass (macOS 26 Tahoe) с pink+cyan палитрой B2 и внутренним brand-градиентом.

**Architecture:** Дизайн-токены в `Shared/Design/Tokens.swift`. Общая поверхность `BrandSurface` (3-слойная: glass + brand overlay + content). Атомарные компоненты (CategoryPill, PeriodSegment, FloatingIsland, SyncIconButton, Crumb, HeroNumber, FriendRow, Sparkline-variant). Поверх них переписаны `DropdownView`, `StatsWidgetView`, `LargeView`, `StatusItemController`.

**Tech Stack:** SwiftUI (Tahoe `.glassEffect()` API), AppKit (`NSStatusItem` для menu bar), WidgetKit, XCTest. Минимум — macOS 26.

**Spec:** [docs/superpowers/specs/2026-05-23-redesign-design.md](../specs/2026-05-23-redesign-design.md)

---

## Phase 1 — Foundation

### Task 1: Bump deployment target до macOS 26

**Files:**
- Modify: `project.yml:5,12` (deploymentTarget + MACOSX_DEPLOYMENT_TARGET)
- Modify: `README.md` (требования)

- [ ] **Step 1: Поднять deployment target в project.yml**

В `project.yml` поменять обе строки:

```yaml
options:
  bundleIdPrefix: com.sergeytovarov
  deploymentTarget:
    macOS: "26.0"
  createIntermediateGroups: true
  groupSortPosition: top
settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "26.0"
```

- [ ] **Step 2: Перегенерировать xcodeproj**

```bash
cd /Users/sergeytovarov/work/ai-stats && xcodegen generate
```
Expected: `Generated project successfully`.

- [ ] **Step 3: Обновить README.md — секция Требования**

Заменить строку `- macOS 14 Sonoma или новее` на `- macOS 26 Tahoe или новее`.

- [ ] **Step 4: Билд-чек**

```bash
xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -20
```
Expected: `BUILD SUCCEEDED`.

- [ ] **Step 5: Commit**

```bash
git add project.yml ai-stats.xcodeproj README.md
git commit -m "chore: bump minimum macOS to 26 Tahoe для Liquid Glass"
```

---

### Task 2: Design tokens — Colors

**Files:**
- Create: `Shared/Design/Tokens.swift`
- Create: `Tests/StatsAppTests/UI/TokensTests.swift`

- [ ] **Step 1: Написать failing test**

`Tests/StatsAppTests/UI/TokensTests.swift`:

```swift
import XCTest
import SwiftUI
@testable import StatsApp

final class TokensTests: XCTestCase {
    func test_brandPink_resolvesToExpectedRGB() {
        let c = NSColor(BrandColor.pink).usingColorSpace(.sRGB)!
        XCTAssertEqual(c.redComponent, 1.0, accuracy: 0.005)        // 255/255
        XCTAssertEqual(c.greenComponent, 45.0/255.0, accuracy: 0.005)
        XCTAssertEqual(c.blueComponent, 109.0/255.0, accuracy: 0.005)
    }

    func test_brandCyan_resolvesToExpectedRGB() {
        let c = NSColor(BrandColor.cyan).usingColorSpace(.sRGB)!
        XCTAssertEqual(c.redComponent, 0.0, accuracy: 0.005)
        XCTAssertEqual(c.greenComponent, 184.0/255.0, accuracy: 0.005)
        XCTAssertEqual(c.blueComponent, 230.0/255.0, accuracy: 0.005)
    }

    func test_pinkLight_isLighterThanPink() {
        let pink = NSColor(BrandColor.pink).usingColorSpace(.sRGB)!
        let light = NSColor(BrandColor.pinkLight).usingColorSpace(.sRGB)!
        // pinkLight = #FF5FA0, должен быть зеленее (95 > 45)
        XCTAssertGreaterThan(light.greenComponent, pink.greenComponent)
    }
}
```

- [ ] **Step 2: Запустить — fail**

```bash
xcodebuild test -project ai-stats.xcodeproj -scheme StatsApp -only-testing:StatsAppTests/TokensTests 2>&1 | tail -10
```
Expected: ошибка компиляции `BrandColor` не определён.

- [ ] **Step 3: Создать Shared/Design/Tokens.swift**

```swift
import SwiftUI

/// Brand colors из spec'а 2026-05-23-redesign-design.md.
enum BrandColor {
    /// #FF2D6D — primary brand
    static let pink = Color(red: 255/255, green: 45/255, blue: 109/255)
    /// #FF5FA0 — top gradient stop для активных pill и hero number
    static let pinkLight = Color(red: 255/255, green: 95/255, blue: 160/255)
    /// #00B8E6 — secondary brand
    static let cyan = Color(red: 0, green: 184/255, blue: 230/255)
    /// #4FE6FF — light cyan для accent и gradient
    static let cyanLight = Color(red: 79/255, green: 230/255, blue: 255/255)
    /// #00FF9D — success (используется только когда cyan не подходит)
    static let success = Color(red: 0, green: 255/255, blue: 157/255)
    /// #FF453A — danger
    static let danger = Color(red: 255/255, green: 69/255, blue: 58/255)
}

/// Surface colors (полупрозрачные подложки и оверлеи).
enum SurfaceColor {
    /// rgba(20,8,30,0.55) — base подложка стекла
    static let base = Color(red: 20/255, green: 8/255, blue: 30/255).opacity(0.55)
    /// Pink tint для overlay top-left
    static let tintPink = BrandColor.pink.opacity(0.42)
    /// Cyan tint для overlay bottom-right
    static let tintCyan = BrandColor.cyan.opacity(0.32)
    /// rgba(255,255,255,0.22) — edge stroke
    static let borderGlass = Color.white.opacity(0.22)
    /// rgba(255,255,255,0.08) — внутренние разделители
    static let dividerSubtle = Color.white.opacity(0.08)
}

/// Text colors (поверх стекла).
enum TextColor {
    /// чисто-белый
    static let primary = Color.white
    /// 70% — метаданные
    static let secondary = Color.white.opacity(0.7)
    /// 50% — sync timestamp, лейблы 4-го уровня
    static let muted = Color.white.opacity(0.5)
    /// pink lighter для AI crumb
    static let crumbAI = Color(red: 255/255, green: 143/255, blue: 184/255).opacity(0.9)
    /// cyan для GitHub crumb
    static let crumbGitHub = BrandColor.cyanLight.opacity(0.9)
    /// нейтрал для Друзей
    static let crumbFriends = Color.white.opacity(0.7)
}
```

- [ ] **Step 4: Запустить тест — pass**

```bash
xcodebuild test -project ai-stats.xcodeproj -scheme StatsApp -only-testing:StatsAppTests/TokensTests 2>&1 | tail -10
```
Expected: `Test Suite 'TokensTests' passed`.

- [ ] **Step 5: Commit**

```bash
git add Shared/Design/Tokens.swift Tests/StatsAppTests/UI/TokensTests.swift project.yml
git commit -m "feat(design): добавить brand-токены — pink/cyan дуэт"
```

> ⚠️ Если xcodegen не подцепил новые файлы — перегенерировать: `xcodegen generate`.

---

### Task 3: Design tokens — Typography + Spacing + Radii

**Files:**
- Modify: `Shared/Design/Tokens.swift` (расширить)

- [ ] **Step 1: Добавить в Tokens.swift**

В конец файла:

```swift
/// Font tokens — все system, SF Pro Display для display.*, SF Pro Text для остального.
enum BrandFont {
    static let displayXXL = Font.system(size: 44, weight: .bold, design: .default).monospacedDigit()
    static let displayXL = Font.system(size: 46, weight: .bold, design: .default).monospacedDigit()
    static let displayL = Font.system(size: 38, weight: .bold, design: .default).monospacedDigit()
    static let displayM = Font.system(size: 30, weight: .bold, design: .default).monospacedDigit()

    static let unitL = Font.system(size: 18, weight: .semibold)
    static let delta = Font.system(size: 12, weight: .semibold).monospacedDigit()
    static let body = Font.system(size: 13, weight: .regular)
    static let caption = Font.system(size: 11, weight: .medium)
    static let crumb = Font.system(size: 11, weight: .semibold)
    static let lbl = Font.system(size: 10, weight: .semibold)
    static let pillBody = Font.system(size: 11, weight: .semibold)
    static let pillPeriod = Font.system(size: 10, weight: .semibold)
}

/// Spacing — 4pt grid.
enum BrandSpacing {
    static let xs: CGFloat = 4
    static let s: CGFloat = 6
    static let m: CGFloat = 8
    static let l: CGFloat = 12
    static let xl: CGFloat = 16
    static let xxl: CGFloat = 22

    /// Резерв снизу popover'а под floating island (~ 46 + 32 воздуха).
    static let islandClearance: CGFloat = 78
    /// Островок offset от bottom edge.
    static let islandBottomOffset: CGFloat = 14
}

/// Radii.
enum BrandRadius {
    static let surface: CGFloat = 22
    static let pill: CGFloat = 999       // capsule
    static let button: CGFloat = 9
    static let field: CGFloat = 10
}
```

- [ ] **Step 2: Билд-чек**

```bash
xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
```
Expected: `BUILD SUCCEEDED`.

- [ ] **Step 3: Commit**

```bash
git add Shared/Design/Tokens.swift
git commit -m "feat(design): добавить токены typography/spacing/radii"
```

---

### Task 4: Widget number formatter (без копеек)

**Files:**
- Modify: `Shared/Util/` (или там где formatters; проверить — если нет общего файла, создать `Shared/Design/MoneyFormatter.swift`)
- Create: `Tests/StatsAppTests/UI/MoneyFormatterTests.swift`

- [ ] **Step 1: Найти где сейчас форматируется `$%.2f`**

```bash
grep -rn '\$%.2f\|\$%\.2f' /Users/sergeytovarov/work/ai-stats --include="*.swift" | head -10
```

Запомни список — там копейки в поповере (оставляем) и виджетах (убираем).

- [ ] **Step 2: Написать failing test**

`Tests/StatsAppTests/UI/MoneyFormatterTests.swift`:

```swift
import XCTest
@testable import StatsApp

final class MoneyFormatterTests: XCTestCase {
    func test_widgetMoney_roundsToInteger_noDecimals() {
        XCTAssertEqual(MoneyFormatter.widget(1602.78), "$1 602")
        XCTAssertEqual(MoneyFormatter.widget(389.69), "$389")
        XCTAssertEqual(MoneyFormatter.widget(9.95), "$10")
        XCTAssertEqual(MoneyFormatter.widget(0.49), "$0")
        XCTAssertEqual(MoneyFormatter.widget(0.5), "$1")
    }

    func test_widgetDelta_keepsSignAndRounds() {
        XCTAssertEqual(MoneyFormatter.widgetDelta(389.69), "+$389")
        XCTAssertEqual(MoneyFormatter.widgetDelta(-50.40), "−$50")
        XCTAssertEqual(MoneyFormatter.widgetDelta(0), "$0")
    }

    func test_popoverMoney_keepsCents() {
        XCTAssertEqual(MoneyFormatter.popover(1602.78), "$1 602.78")
        XCTAssertEqual(MoneyFormatter.popover(0.49), "$0.49")
    }
}
```

- [ ] **Step 3: Запустить — fail**

```bash
xcodebuild test -project ai-stats.xcodeproj -scheme StatsApp -only-testing:StatsAppTests/MoneyFormatterTests 2>&1 | tail -10
```
Expected: ошибка компиляции `MoneyFormatter`.

- [ ] **Step 4: Создать Shared/Design/MoneyFormatter.swift**

```swift
import Foundation

/// Форматтеры денег для разных surfaces.
/// - Поповер: с копейками.
/// - Виджеты: целые доллары.
enum MoneyFormatter {
    private static let group: NumberFormatter = {
        let f = NumberFormatter()
        f.numberStyle = .decimal
        f.groupingSeparator = "\u{00A0}"  // non-breaking space
        f.maximumFractionDigits = 0
        return f
    }()

    private static let groupCents: NumberFormatter = {
        let f = NumberFormatter()
        f.numberStyle = .decimal
        f.groupingSeparator = "\u{00A0}"
        f.minimumFractionDigits = 2
        f.maximumFractionDigits = 2
        return f
    }()

    /// "$1 602" — для виджетов.
    static func widget(_ value: Double) -> String {
        let rounded = value.rounded()
        let s = group.string(from: NSNumber(value: rounded)) ?? "0"
        return "$" + s
    }

    /// "+$389" / "−$50" — дельта в виджетах.
    static func widgetDelta(_ value: Double) -> String {
        let rounded = value.rounded()
        if rounded == 0 { return "$0" }
        let abs = group.string(from: NSNumber(value: Swift.abs(rounded))) ?? "0"
        let sign = rounded > 0 ? "+" : "−"   // U+2212 minus
        return "\(sign)$\(abs)"
    }

    /// "$1 602.78" — для поповера.
    static func popover(_ value: Double) -> String {
        let s = groupCents.string(from: NSNumber(value: value)) ?? "0.00"
        return "$" + s
    }
}
```

- [ ] **Step 5: Запустить — pass**

```bash
xcodegen generate && xcodebuild test -project ai-stats.xcodeproj -scheme StatsApp -only-testing:StatsAppTests/MoneyFormatterTests 2>&1 | tail -10
```
Expected: `Test Suite 'MoneyFormatterTests' passed`.

- [ ] **Step 6: Commit**

```bash
git add Shared/Design/MoneyFormatter.swift Tests/StatsAppTests/UI/MoneyFormatterTests.swift
git commit -m "feat(design): MoneyFormatter — копейки в поповере, без копеек в виджетах"
```

---

## Phase 2 — Surface system

### Task 5: BrandSurface (3-layer glass + brand overlay)

**Files:**
- Create: `Shared/Design/BrandSurface.swift`

- [ ] **Step 1: Создать Shared/Design/BrandSurface.swift**

```swift
import SwiftUI

/// 3-слойная поверхность: Liquid Glass (Tahoe) + внутренний brand-градиент + content.
/// Идентичность держится независимо от обоев.
///
/// Использование:
/// ```swift
/// VStack { ... }
///     .brandSurface()
/// ```
struct BrandSurface<Content: View>: View {
    let cornerRadius: CGFloat
    let tintIntensity: Double  // 0...1, дефолт 1.0
    let content: Content

    init(
        cornerRadius: CGFloat = BrandRadius.surface,
        tintIntensity: Double = 1.0,
        @ViewBuilder content: () -> Content
    ) {
        self.cornerRadius = cornerRadius
        self.tintIntensity = tintIntensity
        self.content = content()
    }

    var body: some View {
        ZStack {
            // Layer 1: base glass (Tahoe API)
            RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                .fill(SurfaceColor.base)
                .glassEffect(in: RoundedRectangle(cornerRadius: cornerRadius, style: .continuous))

            // Layer 2: brand overlay — radial pink TL + radial cyan BR
            RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                .fill(
                    RadialGradient(
                        gradient: Gradient(colors: [
                            SurfaceColor.tintPink.opacity(tintIntensity),
                            .clear
                        ]),
                        center: UnitPoint(x: 0.15, y: 0.0),
                        startRadius: 0,
                        endRadius: 280
                    )
                )
                .blendMode(.plusLighter)
                .allowsHitTesting(false)

            RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                .fill(
                    RadialGradient(
                        gradient: Gradient(colors: [
                            SurfaceColor.tintCyan.opacity(tintIntensity),
                            .clear
                        ]),
                        center: UnitPoint(x: 1.0, y: 1.0),
                        startRadius: 0,
                        endRadius: 320
                    )
                )
                .blendMode(.plusLighter)
                .allowsHitTesting(false)

            // Layer 3: content
            content
        }
        .overlay(
            RoundedRectangle(cornerRadius: cornerRadius, style: .continuous)
                .strokeBorder(SurfaceColor.borderGlass, lineWidth: 0.5)
        )
        .clipShape(RoundedRectangle(cornerRadius: cornerRadius, style: .continuous))
        .shadow(color: .black.opacity(0.55), radius: 20, x: 0, y: 16)
    }
}

extension View {
    /// Применяет brand-поверхность (стекло + brand overlay) ко всему контенту.
    func brandSurface(cornerRadius: CGFloat = BrandRadius.surface, tintIntensity: Double = 1.0) -> some View {
        BrandSurface(cornerRadius: cornerRadius, tintIntensity: tintIntensity) {
            self
        }
    }
}
```

> ⚠️ Если `.glassEffect()` API отличается в финальной Tahoe — проверить по [Apple docs SwiftUI Glass](https://developer.apple.com/documentation/swiftui). На момент plan'а это базовый Tahoe API, сигнатура `.glassEffect(in:)`. Если имя другое — заменить, но архитектура (3 слоя ZStack) остаётся.

- [ ] **Step 2: Билд-чек**

```bash
xcodegen generate && xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
```
Expected: `BUILD SUCCEEDED`.

- [ ] **Step 3: Commit**

```bash
git add Shared/Design/BrandSurface.swift
git commit -m "feat(design): BrandSurface — 3-слойная glass+brand-overlay поверхность"
```

---

### Task 6: Smoke-проверка BrandSurface визуально

**Files:**
- Create temporary: `StatsApp/Sources/_SurfacePreview.swift` (preview-only)

- [ ] **Step 1: Создать preview-файл**

```swift
import SwiftUI

#if DEBUG
struct _SurfacePreview: PreviewProvider {
    static var previews: some View {
        VStack(spacing: 20) {
            // На тёмном фоне
            ZStack {
                Color(red: 0.05, green: 0.03, blue: 0.06)
                VStack(alignment: .leading) {
                    Text("AI · СЕГОДНЯ").font(BrandFont.crumb).foregroundStyle(TextColor.crumbAI)
                    Text("$1 602.78").font(BrandFont.displayXXL).foregroundStyle(.white)
                }
                .padding(18)
                .frame(width: 360, height: 200, alignment: .topLeading)
                .brandSurface()
                .padding(40)
            }

            // На светлом фоне
            ZStack {
                Color.white
                VStack(alignment: .leading) {
                    Text("AI · СЕГОДНЯ").font(BrandFont.crumb).foregroundStyle(TextColor.crumbAI)
                    Text("$1 602.78").font(BrandFont.displayXXL).foregroundStyle(.white)
                }
                .padding(18)
                .frame(width: 360, height: 200, alignment: .topLeading)
                .brandSurface()
                .padding(40)
            }
        }
        .previewLayout(.sizeThatFits)
    }
}
#endif
```

- [ ] **Step 2: Открыть в Xcode и убедиться визуально**

```bash
open /Users/sergeytovarov/work/ai-stats/ai-stats.xcodeproj
```
В Xcode открыть `_SurfacePreview.swift`, включить Canvas (⌥⌘↩). Проверить: pink TL + cyan BR видны на обоих фонах. Если нет — подкрутить opacity в `SurfaceColor.tintPink/tintCyan` или endRadius в BrandSurface.

- [ ] **Step 3: Удалить preview-файл (или оставить под #if DEBUG — окей оставить)**

Если оставляем — следующий шаг пропускаем. Иначе:

```bash
rm /Users/sergeytovarov/work/ai-stats/StatsApp/Sources/_SurfacePreview.swift && xcodegen generate
```

- [ ] **Step 4: Commit (если файл остаётся)**

```bash
git add StatsApp/Sources/_SurfacePreview.swift
git commit -m "chore(design): preview для визуальной проверки BrandSurface"
```

---

## Phase 3 — Атомарные компоненты

### Task 7: Crumb view

**Files:**
- Create: `Shared/Design/Crumb.swift`
- Create: `Tests/StatsAppTests/UI/CrumbCategoryTests.swift`

- [ ] **Step 1: Failing test для category → color mapping**

```swift
import XCTest
import SwiftUI
@testable import StatsApp

final class CrumbCategoryTests: XCTestCase {
    func test_aiCategory_usesPinkCrumb() {
        XCTAssertEqual(CrumbCategory.ai.color, TextColor.crumbAI)
    }
    func test_githubCategory_usesCyanCrumb() {
        XCTAssertEqual(CrumbCategory.github.color, TextColor.crumbGitHub)
    }
    func test_friendsCategory_usesNeutralCrumb() {
        XCTAssertEqual(CrumbCategory.friends.color, TextColor.crumbFriends)
    }
}
```

- [ ] **Step 2: Запустить — fail**

```bash
xcodegen generate && xcodebuild test -project ai-stats.xcodeproj -scheme StatsApp -only-testing:StatsAppTests/CrumbCategoryTests 2>&1 | tail -10
```
Expected: `CrumbCategory` не определён.

- [ ] **Step 3: Создать Shared/Design/Crumb.swift**

```swift
import SwiftUI

enum CrumbCategory: Equatable {
    case ai, github, friends

    var color: Color {
        switch self {
        case .ai: return TextColor.crumbAI
        case .github: return TextColor.crumbGitHub
        case .friends: return TextColor.crumbFriends
        }
    }
}

/// "AI · Сегодня" — uppercase, tracked, цветной по категории.
struct Crumb: View {
    let category: CrumbCategory
    let title: String
    let period: String   // локализованный «Сегодня» / «Неделя» / «Месяц»

    var body: some View {
        Text("\(title) · \(period)")
            .font(BrandFont.crumb)
            .tracking(1.0)
            .textCase(.uppercase)
            .foregroundStyle(category.color)
    }
}
```

- [ ] **Step 4: Запустить — pass**

```bash
xcodebuild test -project ai-stats.xcodeproj -scheme StatsApp -only-testing:StatsAppTests/CrumbCategoryTests 2>&1 | tail -10
```
Expected: passed.

- [ ] **Step 5: Commit**

```bash
git add Shared/Design/Crumb.swift Tests/StatsAppTests/UI/CrumbCategoryTests.swift
git commit -m "feat(design): Crumb view с цветом по категории"
```

---

### Task 8: HeroNumber view

**Files:**
- Create: `Shared/Design/HeroNumber.swift`

- [ ] **Step 1: Создать**

```swift
import SwiftUI

enum HeroVariant {
    case pink   // AI
    case cyan   // GitHub
}

/// Главное число поповера/виджета с градиентным fill.
struct HeroNumber: View {
    let text: String
    let font: Font
    let variant: HeroVariant

    init(_ text: String, font: Font = BrandFont.displayXXL, variant: HeroVariant = .pink) {
        self.text = text
        self.font = font
        self.variant = variant
    }

    var body: some View {
        Text(text)
            .font(font)
            .foregroundStyle(gradient)
    }

    private var gradient: LinearGradient {
        let colors: [Color]
        switch variant {
        case .pink:
            colors = [.white, Color(red: 1.0, green: 212/255, blue: 227/255)]
        case .cyan:
            colors = [.white, Color(red: 212/255, green: 243/255, blue: 1.0)]
        }
        return LinearGradient(
            gradient: Gradient(stops: [
                .init(color: colors[0], location: 0.3),
                .init(color: colors[1], location: 1.0)
            ]),
            startPoint: .top,
            endPoint: .bottom
        )
    }
}

/// "28 коммитов" — число + единица рядом.
struct HeroNumberWithUnit: View {
    let number: String
    let unit: String   // "коммитов", "репозиториев" — локализовано на месте использования

    var body: some View {
        HStack(alignment: .firstTextBaseline, spacing: 8) {
            HeroNumber(number, font: .system(size: 56, weight: .bold).monospacedDigit(), variant: .cyan)
            Text(unit).font(BrandFont.unitL).foregroundStyle(BrandColor.cyanLight.opacity(0.85))
        }
    }
}
```

- [ ] **Step 2: Билд-чек**

```bash
xcodegen generate && xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
```
Expected: `BUILD SUCCEEDED`.

- [ ] **Step 3: Commit**

```bash
git add Shared/Design/HeroNumber.swift
git commit -m "feat(design): HeroNumber с градиентным fill (pink/cyan вариант)"
```

---

### Task 9: CategoryPill + PeriodSegment + FloatingIsland

**Files:**
- Create: `StatsApp/Status/IslandControl.swift`

- [ ] **Step 1: Создать IslandControl.swift со всеми тремя view**

```swift
import SwiftUI

// MARK: - CategoryPill

struct CategoryPill: View {
    let title: String
    let isActive: Bool
    let action: () -> Void

    var body: some View {
        Button(action: action) {
            Text(title)
                .font(BrandFont.pillBody)
                .padding(.vertical, 7)
                .padding(.horizontal, 12)
                .foregroundStyle(isActive ? Color.white : Color.white.opacity(0.6))
                .background(
                    Group {
                        if isActive {
                            LinearGradient(
                                colors: [BrandColor.pinkLight, BrandColor.pink],
                                startPoint: .top, endPoint: .bottom
                            )
                            .overlay(
                                RoundedRectangle(cornerRadius: BrandRadius.pill)
                                    .stroke(Color.white.opacity(0.35), lineWidth: 0.5)
                                    .blendMode(.plusLighter)
                            )
                        } else {
                            Color.clear
                        }
                    }
                )
                .clipShape(RoundedRectangle(cornerRadius: BrandRadius.pill, style: .continuous))
                .shadow(color: isActive ? BrandColor.pink.opacity(0.45) : .clear, radius: 6, x: 0, y: 4)
        }
        .buttonStyle(.plain)
        .accessibilityAddTraits(isActive ? [.isSelected] : [])
    }
}

// MARK: - PeriodSegment

struct PeriodSegment: View {
    @Binding var selection: Period

    var body: some View {
        HStack(spacing: 1) {
            ForEach(Period.allCases) { p in
                Button {
                    selection = p
                } label: {
                    Text(p.shortKey)   // "Д" / "Н" / "М" — локализованно
                        .font(BrandFont.pillPeriod)
                        .frame(minWidth: 16)
                        .padding(.vertical, 5)
                        .padding(.horizontal, 9)
                        .foregroundStyle(p == selection ? Color(red: 0, green: 40/255, blue: 46/255) : Color.white.opacity(0.55))
                        .background(
                            Group {
                                if p == selection {
                                    LinearGradient(colors: [BrandColor.cyanLight, BrandColor.cyan], startPoint: .top, endPoint: .bottom)
                                } else { Color.clear }
                            }
                        )
                        .clipShape(RoundedRectangle(cornerRadius: BrandRadius.pill, style: .continuous))
                        .shadow(color: p == selection ? BrandColor.cyan.opacity(0.45) : .clear, radius: 4, x: 0, y: 2)
                }
                .buttonStyle(.plain)
            }
        }
        .padding(2)
        .background(Color.white.opacity(0.06))
        .clipShape(RoundedRectangle(cornerRadius: BrandRadius.pill, style: .continuous))
    }
}

// MARK: - FloatingIsland

struct FloatingIsland: View {
    @Binding var section: DropdownSection
    @Binding var period: Period

    var body: some View {
        HStack(spacing: 2) {
            CategoryPill(title: NSLocalizedString("section.ai", comment: ""),
                         isActive: section == .ai) { section = .ai }
            CategoryPill(title: NSLocalizedString("section.github", comment: ""),
                         isActive: section == .github) { section = .github }
            CategoryPill(title: NSLocalizedString("section.friends", comment: ""),
                         isActive: section == .leaderboard) { section = .leaderboard }

            // separator
            Rectangle().fill(Color.white.opacity(0.18))
                .frame(width: 0.5, height: 18)
                .padding(.horizontal, 4)

            PeriodSegment(selection: $period)
        }
        .padding(4)
        .background(
            RoundedRectangle(cornerRadius: BrandRadius.pill, style: .continuous)
                .fill(Color.black.opacity(0.65))
                .glassEffect(in: RoundedRectangle(cornerRadius: BrandRadius.pill, style: .continuous))
        )
        .overlay(
            RoundedRectangle(cornerRadius: BrandRadius.pill, style: .continuous)
                .stroke(Color.white.opacity(0.22), lineWidth: 0.5)
        )
        .shadow(color: .black.opacity(0.65), radius: 16, x: 0, y: 12)
        .shadow(color: BrandColor.pink.opacity(0.14), radius: 14, x: 0, y: 0)
    }
}
```

> ⚠️ `Period.shortKey` нужно добавить — см. шаг 2.

- [ ] **Step 2: Добавить `shortKey` в Shared/Period.swift**

```bash
grep -n "titleKey" /Users/sergeytovarov/work/ai-stats/Shared/Period.swift
```

Рядом с `titleKey` добавить:

```swift
var shortKey: String {
    switch self {
    case .day: return NSLocalizedString("period.short.day", comment: "")    // "Д"
    case .week: return NSLocalizedString("period.short.week", comment: "")  // "Н"
    case .month: return NSLocalizedString("period.short.month", comment: "") // "М"
    }
}
```

- [ ] **Step 3: Добавить локализации**

В `Shared/Resources/ru.lproj/Localizable.strings`:
```
"period.short.day" = "Д";
"period.short.week" = "Н";
"period.short.month" = "М";
"section.ai" = "AI";
"section.friends" = "Друзья";
```

В `Shared/Resources/en.lproj/Localizable.strings`:
```
"period.short.day" = "D";
"period.short.week" = "W";
"period.short.month" = "M";
"section.ai" = "AI";
"section.friends" = "Friends";
```

(Сохраняем `section.leaderboard` ключ — он останется только в виджете LargeView с подменой текста, если нужно.)

- [ ] **Step 4: Билд-чек**

```bash
xcodegen generate && xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
```
Expected: `BUILD SUCCEEDED`. Если ругается на `.friends` case в `DropdownSection` — секция называется `.leaderboard` в коде, мы её НЕ переименовываем (см. spec § «только в UI»). В `FloatingIsland` мы передаём `section.leaderboard` пилюлю с label «Друзья», то есть локализовано. Так что test файла `.leaderboard` остаётся.

- [ ] **Step 5: Commit**

```bash
git add StatsApp/Status/IslandControl.swift Shared/Period.swift Shared/Resources/ru.lproj/Localizable.strings Shared/Resources/en.lproj/Localizable.strings
git commit -m "feat(design): FloatingIsland + CategoryPill + PeriodSegment"
```

---

### Task 10: SyncIconButton

**Files:**
- Create: `Shared/Design/SyncIconButton.swift`

- [ ] **Step 1: Создать**

```swift
import SwiftUI

struct SyncIconButton: View {
    let systemImage: String
    let action: () -> Void
    @State private var hovering = false

    var body: some View {
        Button(action: action) {
            Image(systemName: systemImage)
                .font(.system(size: 15, weight: .semibold))
                .foregroundStyle(Color.white.opacity(0.95))
                .frame(width: 28, height: 28)
                .background(
                    RoundedRectangle(cornerRadius: BrandRadius.button, style: .continuous)
                        .fill(Color.white.opacity(hovering ? 0.14 : 0.08))
                )
                .overlay(
                    RoundedRectangle(cornerRadius: BrandRadius.button, style: .continuous)
                        .stroke(Color.white.opacity(0.12), lineWidth: 0.5)
                )
        }
        .buttonStyle(.plain)
        .onHover { hovering = $0 }
    }
}
```

- [ ] **Step 2: Билд-чек + commit**

```bash
xcodegen generate && xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
git add Shared/Design/SyncIconButton.swift
git commit -m "feat(design): SyncIconButton 28×28 с hover"
```

---

### Task 11: FriendRow

**Files:**
- Create: `Shared/Design/FriendRow.swift`

- [ ] **Step 1: Создать**

```swift
import SwiftUI

struct FriendRow: View {
    let rank: Int
    let name: String
    let valueText: String
    let isMe: Bool

    var body: some View {
        HStack(spacing: 8) {
            Text("\(rank).")
                .font(BrandFont.body)
                .foregroundStyle(.white.opacity(0.5))
                .frame(width: 22, alignment: .leading)
                .monospacedDigit()

            Circle()
                .fill(avatarGradient)
                .frame(width: 22, height: 22)
                .shadow(color: isMe ? BrandColor.pink.opacity(0.5) : .clear, radius: 5)

            Text(name)
                .font(.system(size: 14, weight: isMe ? .semibold : .medium))
                .foregroundStyle(isMe ? TextColor.crumbAI : Color.white)
                .lineLimit(1)
                .truncationMode(.tail)

            Spacer()

            Text(valueText)
                .font(BrandFont.body)
                .monospacedDigit()
                .foregroundStyle(.white.opacity(0.78))
        }
        .padding(.vertical, 5)
    }

    private var avatarGradient: LinearGradient {
        if isMe {
            return LinearGradient(colors: [BrandColor.pinkLight, BrandColor.pink], startPoint: .topLeading, endPoint: .bottomTrailing)
        } else {
            return LinearGradient(
                colors: [BrandColor.pinkLight.opacity(0.8), BrandColor.cyanLight.opacity(0.8)],
                startPoint: .topLeading, endPoint: .bottomTrailing
            )
        }
    }
}
```

- [ ] **Step 2: Билд + commit**

```bash
xcodegen generate && xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
git add Shared/Design/FriendRow.swift
git commit -m "feat(design): FriendRow — ранг, аватар, имя, значение"
```

---

### Task 12: Sparkline rewrite — variant API + gradient stroke

**Files:**
- Modify: `StatsApp/Status/Sparkline.swift`
- Create: `Tests/StatsAppTests/UI/SparklineVariantTests.swift`

- [ ] **Step 1: Failing test для variant**

```swift
import XCTest
import SwiftUI
@testable import StatsApp

final class SparklineVariantTests: XCTestCase {
    func test_aiVariant_usesPinkColors() {
        let v = SparklineVariant.ai
        XCTAssertEqual(v.strokeColors.count, 2)
        let first = NSColor(v.strokeColors[0]).usingColorSpace(.sRGB)!
        XCTAssertEqual(first.greenComponent, 95/255.0, accuracy: 0.005)  // #FF5FA0
    }

    func test_githubVariant_usesCyanColors() {
        let v = SparklineVariant.github
        let first = NSColor(v.strokeColors[0]).usingColorSpace(.sRGB)!
        XCTAssertEqual(first.greenComponent, 230/255.0, accuracy: 0.005)  // #4FE6FF
    }
}
```

- [ ] **Step 2: Запустить — fail**

```bash
xcodegen generate && xcodebuild test -project ai-stats.xcodeproj -scheme StatsApp -only-testing:StatsAppTests/SparklineVariantTests 2>&1 | tail -10
```

- [ ] **Step 3: Перезаписать Sparkline.swift**

```swift
import SwiftUI
import Charts

enum SparklineVariant {
    case ai
    case github

    var strokeColors: [Color] {
        switch self {
        case .ai:     return [BrandColor.pinkLight, BrandColor.pink]
        case .github: return [BrandColor.cyanLight, BrandColor.cyan]
        }
    }

    var fillTopColor: Color {
        switch self {
        case .ai:     return BrandColor.pinkLight.opacity(0.3)
        case .github: return BrandColor.cyanLight.opacity(0.28)
        }
    }
}

struct Sparkline: View {
    let values: [Double]
    var variant: SparklineVariant = .ai

    var body: some View {
        Chart(Array(values.enumerated()), id: \.offset) { item in
            // fill под линией
            AreaMark(x: .value("Day", item.offset), y: .value("Value", item.element))
                .interpolationMethod(.monotone)
                .foregroundStyle(
                    LinearGradient(
                        colors: [variant.fillTopColor, .clear],
                        startPoint: .top, endPoint: .bottom
                    )
                )
            // линия
            LineMark(x: .value("Day", item.offset), y: .value("Value", item.element))
                .interpolationMethod(.monotone)
                .foregroundStyle(
                    LinearGradient(colors: variant.strokeColors, startPoint: .leading, endPoint: .trailing)
                )
                .lineStyle(StrokeStyle(lineWidth: 1.6, lineCap: .round))
        }
        .chartXAxis(.hidden)
        .chartYAxis(.hidden)
        .frame(height: 36)
    }
}

extension Color {
    /// GitHub contribution heatmap green (#39d353). Оставлено для возможной обратной совместимости.
    static let githubGreen = Color(red: 0x39 / 255.0, green: 0xd3 / 255.0, blue: 0x53 / 255.0)
}
```

- [ ] **Step 4: Запустить тесты — pass**

```bash
xcodebuild test -project ai-stats.xcodeproj -scheme StatsApp -only-testing:StatsAppTests/SparklineVariantTests 2>&1 | tail -10
```
Expected: passed.

- [ ] **Step 5: Если есть call sites старого `Sparkline(values:tint:)` — обновить**

```bash
grep -rn "Sparkline(values" /Users/sergeytovarov/work/ai-stats --include="*.swift"
```
Все вызовы заменить на `Sparkline(values: ..., variant: .ai)` или `.github`. Полный билд-чек:

```bash
xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
```

- [ ] **Step 6: Commit**

```bash
git add StatsApp/Status/Sparkline.swift Tests/StatsAppTests/UI/SparklineVariantTests.swift
git add -u
git commit -m "feat(design): Sparkline с variant API (ai/github) и gradient stroke"
```

---

## Phase 4 — Popover

### Task 13: DropdownView skeleton — BrandSurface + FloatingIsland

**Files:**
- Modify: `StatsApp/Status/DropdownView.swift`

- [ ] **Step 1: Перезаписать DropdownView.swift (skeleton — секции временно через старые view)**

```swift
import SwiftUI

struct DropdownView: View {
    @ObservedObject var viewModel: DropdownViewModel
    let onRefresh: () -> Void
    let onOpenSettings: () -> Void

    var body: some View {
        ZStack(alignment: .bottom) {
            VStack(alignment: .leading, spacing: 0) {
                content
                    .padding(.horizontal, 18)
                    .padding(.top, 16)

                Spacer(minLength: 0)

                Divider().background(SurfaceColor.dividerSubtle).padding(.horizontal, 18)

                HStack {
                    Text(String(format: NSLocalizedString("footer.last_sync %@", comment: ""),
                                viewModel.lastSyncDescription))
                        .font(BrandFont.caption)
                        .foregroundStyle(TextColor.muted)
                    Spacer()
                    SyncIconButton(systemImage: "arrow.clockwise", action: onRefresh)
                    SyncIconButton(systemImage: "gearshape", action: onOpenSettings)
                }
                .padding(.horizontal, 18)
                .padding(.top, 10)
                .padding(.bottom, BrandSpacing.islandClearance)   // место под остров
            }
            .frame(width: 400, height: 560, alignment: .topLeading)
            .brandSurface()

            FloatingIsland(section: $viewModel.section, period: $viewModel.period)
                .padding(.bottom, BrandSpacing.islandBottomOffset)
        }
        .frame(width: 400, height: 560)
        .task { await viewModel.loadLeaderboard() }
    }

    @ViewBuilder private var content: some View {
        switch viewModel.section {
        case .ai:
            DropdownAISection(viewModel: viewModel)
        case .github:
            DropdownGitHubSection(viewModel: viewModel)
        case .leaderboard:
            DropdownLeaderboardSection(viewModel: viewModel)
        }
    }
}
```

> Старые секции пока остаются — их перепишем в Task 14–16.

- [ ] **Step 2: Билд-чек**

```bash
xcodegen generate && xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
```
Expected: `BUILD SUCCEEDED`.

- [ ] **Step 3: Запустить app и убедиться, что поповер открывается с островком**

```bash
xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/
open build/Build/Products/Debug/StatsApp.app
```

Кликнуть menu bar item — должен открыться поповер с FloatingIsland снизу. Контент пока в старом стиле — это нормально.

- [ ] **Step 4: Commit**

```bash
git add StatsApp/Status/DropdownView.swift
git commit -m "refactor(popover): DropdownView на BrandSurface + FloatingIsland"
```

---

### Task 14: AISection rewrite

**Files:**
- Modify: `StatsApp/Status/DropdownSections.swift` (только `DropdownAISection`)

- [ ] **Step 1: Перезаписать DropdownAISection**

В `DropdownSections.swift` найти `struct DropdownAISection` и заменить полностью на:

```swift
struct DropdownAISection: View {
    @ObservedObject var viewModel: DropdownViewModel

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            Crumb(category: .ai, title: "AI", period: viewModel.period.localizedTitle)

            HeroNumber(MoneyFormatter.popover(viewModel.aiTotals.totalCost), variant: .pink)
                .padding(.top, 4)

            CostDelta(
                current: viewModel.aiTotals.totalCost,
                previous: viewModel.aiTotalsPrev.totalCost,
                period: viewModel.period
            )
            .foregroundStyle(BrandColor.cyanLight)
            .padding(.top, 4)

            Text(DropdownFormat.tokens(viewModel.aiTotals.totalInputTokens + viewModel.aiTotals.totalOutputTokens) + " tokens")
                .font(BrandFont.caption)
                .foregroundStyle(BrandColor.cyanLight.opacity(0.75))
                .padding(.top, 2)

            Text("section.top_models")
                .font(BrandFont.lbl)
                .tracking(1.2)
                .textCase(.uppercase)
                .foregroundStyle(BrandColor.cyanLight.opacity(0.7))
                .padding(.top, 14)
                .padding(.bottom, 6)

            if viewModel.topModels.isEmpty {
                Text("label.no_data").font(BrandFont.caption).foregroundStyle(TextColor.muted)
            } else {
                ForEach(viewModel.topModels.prefix(5), id: \.self) { m in
                    HStack {
                        Text(m.model).font(BrandFont.body)
                        Spacer()
                        Text(MoneyFormatter.popover(m.costUsd))
                            .font(BrandFont.body)
                            .monospacedDigit()
                            .foregroundStyle(.white.opacity(0.8))
                    }
                    .padding(.vertical, 3)
                }
            }

            Spacer(minLength: 14)

            VStack(alignment: .leading, spacing: 4) {
                Text("section.trend")
                    .font(.system(size: 9, weight: .medium))
                    .foregroundStyle(.white.opacity(0.5))
                    .tracking(0.5)
                Sparkline(values: viewModel.aiSparklineValues, variant: .ai)
                    .frame(height: 38)
            }
        }
    }
}
```

> ⚠️ `Period.localizedTitle` нужно добавить — отдельный шаг.

- [ ] **Step 2: Добавить `localizedTitle` в Period.swift**

Найти `titleKey` (там что-то типа `"period.day"`), и рядом:

```swift
var localizedTitle: String {
    NSLocalizedString(titleKey, comment: "")
}
```

- [ ] **Step 3: Билд-чек**

```bash
xcodegen generate && xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
```
Expected: `BUILD SUCCEEDED`. Если `aiSparklineValues` или подобное не существует — проверить ViewModel и заменить на актуальное имя. (Старый код использовал `viewModel.aiSparkline` или `viewModel.daily14`; найти grep'ом.)

```bash
grep -n "Sparkline\|sparkline" /Users/sergeytovarov/work/ai-stats/StatsApp/Status/DropdownViewModel.swift | head
```

- [ ] **Step 4: Commit**

```bash
git add StatsApp/Status/DropdownSections.swift Shared/Period.swift
git commit -m "feat(popover): AISection в новом стиле — Crumb/HeroNumber/Sparkline"
```

---

### Task 15: GitHubSection rewrite (с inline «коммитов»)

**Files:**
- Modify: `StatsApp/Status/DropdownSections.swift` (`DropdownGitHubSection`)

- [ ] **Step 1: Перезаписать `DropdownGitHubSection`**

```swift
struct DropdownGitHubSection: View {
    @ObservedObject var viewModel: DropdownViewModel

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            Crumb(category: .github, title: "GitHub", period: viewModel.period.localizedTitle)

            HeroNumberWithUnit(
                number: "\(viewModel.githubTotals.commits)",
                unit: NSLocalizedString("unit.commits", comment: "")
            )
            .padding(.top, 4)

            Text("+\(viewModel.githubTotals.additions) / −\(viewModel.githubTotals.deletions) " + NSLocalizedString("unit.lines", comment: ""))
                .font(BrandFont.delta)
                .foregroundStyle(BrandColor.cyanLight)
                .padding(.top, 4)

            Text(String(format: NSLocalizedString("unit.repos_active %d", comment: ""), viewModel.githubTotals.activeRepoCount))
                .font(BrandFont.caption)
                .foregroundStyle(TextColor.muted)
                .padding(.top, 2)

            Text("section.top_repos")
                .font(BrandFont.lbl)
                .tracking(1.2)
                .textCase(.uppercase)
                .foregroundStyle(BrandColor.cyanLight.opacity(0.7))
                .padding(.top, 14)
                .padding(.bottom, 6)

            ForEach(viewModel.topRepos.prefix(5), id: \.name) { r in
                HStack {
                    Text(r.name).font(BrandFont.body).lineLimit(1).truncationMode(.middle)
                    Spacer()
                    Text("\(r.commits) c · +\(r.additions)")
                        .font(BrandFont.body)
                        .monospacedDigit()
                        .foregroundStyle(.white.opacity(0.8))
                }
                .padding(.vertical, 3)
            }

            Spacer(minLength: 14)

            VStack(alignment: .leading, spacing: 4) {
                Text("section.trend_additions")
                    .font(.system(size: 9, weight: .medium))
                    .foregroundStyle(.white.opacity(0.5))
                Sparkline(values: viewModel.githubSparklineValues, variant: .github)
                    .frame(height: 38)
            }
        }
    }
}
```

> ⚠️ Имена `githubTotals.commits/additions/deletions/activeRepoCount` и `topRepos`, `githubSparklineValues` — проверить grep'ом, могут отличаться.

```bash
grep -n "commits\|additions\|githubTotals\|topRepos" /Users/sergeytovarov/work/ai-stats/StatsApp/Status/DropdownViewModel.swift | head -20
```

- [ ] **Step 2: Добавить локализации в Shared/Resources/{ru,en}.lproj/Localizable.strings**

RU:
```
"unit.commits" = "коммитов";
"unit.lines" = "строк";
"unit.repos_active %d" = "%d репозиторий активен";
```
EN:
```
"unit.commits" = "commits";
"unit.lines" = "lines";
"unit.repos_active %d" = "%d active repo(s)";
```

> Если `activeRepoCount` отсутствует в данных — добавить вычисление в DropdownViewModel.swift (computed property) или поставить hardcoded "1 репозиторий" для MVP с TODO.

- [ ] **Step 3: Билд-чек**

```bash
xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
```
Expected: `BUILD SUCCEEDED`.

- [ ] **Step 4: Commit**

```bash
git add StatsApp/Status/DropdownSections.swift Shared/Resources/
git add -u
git commit -m "feat(popover): GitHubSection с inline 'коммитов' и cyan-палитрой"
```

---

### Task 16: FriendsSection rewrite (UI label, код — Leaderboard)

**Files:**
- Modify: `StatsApp/Status/DropdownSections.swift` (`DropdownLeaderboardSection`)
- Modify: `Shared/Resources/ru.lproj/Localizable.strings`

- [ ] **Step 1: Поменять локализацию `section.leaderboard`**

В `Shared/Resources/ru.lproj/Localizable.strings`:

```
"section.leaderboard" = "Друзья";
```

В `en.lproj/Localizable.strings`:

```
"section.leaderboard" = "Friends";
```

Также добавить:

```
"label.leaderboard.heading" = "Рейтинг по токенам";    (RU)
"label.leaderboard.heading" = "Token ranking";          (EN)
```

- [ ] **Step 2: Перезаписать `DropdownLeaderboardSection`**

```swift
struct DropdownLeaderboardSection: View {
    @ObservedObject var viewModel: DropdownViewModel

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            Crumb(category: .friends,
                  title: NSLocalizedString("section.leaderboard", comment: ""),
                  period: viewModel.period.localizedTitle)

            Text("label.leaderboard.heading")
                .font(BrandFont.lbl)
                .tracking(1.2)
                .textCase(.uppercase)
                .foregroundStyle(BrandColor.cyanLight.opacity(0.7))
                .padding(.top, 10)
                .padding(.bottom, 6)

            if let error = viewModel.leaderboardError {
                Text(error).font(BrandFont.caption).foregroundStyle(BrandColor.danger)
            } else if viewModel.leaderboard.isEmpty {
                Text("widget.leaderboard.empty")
                    .font(BrandFont.caption)
                    .foregroundStyle(TextColor.muted)
            } else {
                ForEach(Array(viewModel.leaderboard.prefix(10).enumerated()), id: \.offset) { idx, peer in
                    FriendRow(
                        rank: idx + 1,
                        name: peer.displayName,
                        valueText: DropdownFormat.tokens(peer.totalTokens),
                        isMe: peer.isMe
                    )
                }
            }
        }
    }
}
```

> ⚠️ `viewModel.leaderboard` — это уже массив peers; `peer.displayName`, `peer.totalTokens`, `peer.isMe` — проверить grep'ом и заменить на реальные имена.

```bash
grep -n "leaderboard\|peer\|displayName\|isMe" /Users/sergeytovarov/work/ai-stats/StatsApp/Status/DropdownViewModel.swift | head
```

- [ ] **Step 3: Билд-чек**

```bash
xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
```
Expected: `BUILD SUCCEEDED`.

- [ ] **Step 4: Commit**

```bash
git add StatsApp/Status/DropdownSections.swift Shared/Resources/
git commit -m "feat(popover): FriendsSection (UI label) — список друзей с pink-glow моей строки"
```

---

### Task 17: Empty/loading/error states по вкладкам

**Files:**
- Modify: `StatsApp/Status/DropdownSections.swift` (все три секции)

- [ ] **Step 1: Добавить состояния в AI и GitHub секциях**

В каждой секции в начале body добавить guard:

```swift
if viewModel.isLoadingAI {
    StateView(icon: "hourglass", message: NSLocalizedString("state.loading", comment: ""))
} else if viewModel.aiTotals.totalCost == 0 && viewModel.bySource.isEmpty {
    StateView(icon: "play.circle.fill",
              message: NSLocalizedString("state.ai.empty", comment: ""),
              actionLabel: NSLocalizedString("action.run_sync", comment: ""),
              action: viewModel.triggerSync)
}
```

(Аналогично для GitHub — `state.github.no_token`. И Friends — без действия.)

- [ ] **Step 2: Создать StateView**

В новом файле `Shared/Design/StateView.swift`:

```swift
import SwiftUI

struct StateView: View {
    let icon: String
    let message: String
    var actionLabel: String? = nil
    var action: (() -> Void)? = nil

    var body: some View {
        VStack(spacing: 12) {
            Image(systemName: icon)
                .font(.system(size: 28, weight: .light))
                .foregroundStyle(BrandColor.cyanLight.opacity(0.6))
            Text(message)
                .font(BrandFont.body)
                .multilineTextAlignment(.center)
                .foregroundStyle(TextColor.secondary)
            if let actionLabel, let action {
                Button(action: action) {
                    Text(actionLabel)
                        .font(BrandFont.caption)
                        .padding(.horizontal, 14).padding(.vertical, 6)
                        .background(BrandColor.pink.opacity(0.25))
                        .overlay(RoundedRectangle(cornerRadius: 999).stroke(BrandColor.pink.opacity(0.5), lineWidth: 0.5))
                        .clipShape(RoundedRectangle(cornerRadius: 999))
                }
                .buttonStyle(.plain)
                .foregroundStyle(.white)
            }
        }
        .frame(maxWidth: .infinity)
        .padding(.vertical, 40)
    }
}
```

- [ ] **Step 3: Локализации состояний**

В обоих .strings:
RU:
```
"state.loading" = "Загружаем…";
"state.ai.empty" = "ccusage ещё не запускался";
"state.github.no_token" = "GitHub PAT не настроен";
"action.run_sync" = "Запустить sync";
"action.open_settings" = "Открыть настройки";
```
EN:
```
"state.loading" = "Loading…";
"state.ai.empty" = "ccusage hasn't run yet";
"state.github.no_token" = "GitHub PAT is not configured";
"action.run_sync" = "Run sync";
"action.open_settings" = "Open settings";
```

- [ ] **Step 4: Билд + commit**

```bash
xcodegen generate && xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
git add Shared/Design/StateView.swift Shared/Resources/ StatsApp/Status/DropdownSections.swift
git commit -m "feat(popover): empty/loading/error состояния через StateView"
```

> ⚠️ `viewModel.isLoadingAI`, `viewModel.triggerSync()` — если нет, реализовать минимально (computed property; action — вызывать существующий `onRefresh`).

---

## Phase 5 — Menu bar

### Task 18: Capsule status item с gradient

**Files:**
- Modify: `StatsApp/Status/StatusItemController.swift`
- Create: `StatsApp/Status/MenuBarCapsuleView.swift`

- [ ] **Step 1: Посмотреть текущую реализацию**

```bash
cat /Users/sergeytovarov/work/ai-stats/StatsApp/Status/StatusItemController.swift
```

Нас интересует, как сейчас рисуется button — через `image` или `title`. Скорее всего `button.title = "↗ $1602.78"`.

- [ ] **Step 2: Создать MenuBarCapsuleView**

```swift
import SwiftUI

struct MenuBarCapsuleView: View {
    let priceText: String   // "$1,602.78"

    var body: some View {
        HStack(spacing: 4) {
            Image(systemName: "chart.line.uptrend.xyaxis")
                .font(.system(size: 11, weight: .semibold))
            Text(priceText)
                .font(.system(size: 11, weight: .semibold))
                .monospacedDigit()
        }
        .foregroundStyle(.white)
        .padding(.vertical, 4)
        .padding(.horizontal, 10)
        .background(
            LinearGradient(
                colors: [BrandColor.pink.opacity(0.25), BrandColor.cyan.opacity(0.25)],
                startPoint: .leading, endPoint: .trailing
            )
            .clipShape(Capsule())
        )
        .overlay(Capsule().stroke(Color.white.opacity(0.3), lineWidth: 0.5))
    }
}
```

- [ ] **Step 3: Подключить через NSHostingView в StatusItemController.swift**

Заменить кусок с `button.title = ...` на использование NSHostingView:

```swift
// В месте, где сейчас обновляется текст status item button:
let hosting = NSHostingView(rootView: MenuBarCapsuleView(priceText: formatted))
hosting.frame.size = hosting.intrinsicContentSize
button.subviews.forEach { $0.removeFromSuperview() }
button.addSubview(hosting)
button.frame.size = hosting.frame.size
button.title = ""   // на всякий
```

(Точное место — где сейчас обновляется button. Найти grep'ом `button.title` в StatusItemController.swift.)

- [ ] **Step 4: Билд + Run + визуальная проверка**

```bash
xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Debug -derivedDataPath build/
open build/Build/Products/Debug/StatsApp.app
```
В menu bar должна появиться capsule с pink→cyan градиентом.

- [ ] **Step 5: Commit**

```bash
git add StatsApp/Status/MenuBarCapsuleView.swift StatsApp/Status/StatusItemController.swift
git commit -m "feat(menubar): capsule с pink→cyan gradient вместо plain title"
```

---

## Phase 6 — Widgets

### Task 19: Small widget rewrite

**Files:**
- Modify: `StatsWidget/Views/StatsWidgetView.swift` (там Small + Medium)

- [ ] **Step 1: Прочитать текущий код**

```bash
cat /Users/sergeytovarov/work/ai-stats/StatsWidget/Views/StatsWidgetView.swift
```

- [ ] **Step 2: Найти SmallView (или соответствующий блок) и заменить**

```swift
struct SmallView: View {
    let entry: StatsTimelineProvider.Entry

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            Crumb(category: .ai, title: "AI", period: entry.configuration.period.localizedTitle)

            HeroNumber(MoneyFormatter.widget(entry.snapshot.aiCost), font: BrandFont.displayM, variant: .pink)
                .padding(.top, 6)

            if let delta = entry.snapshot.aiCostDelta {
                Text(MoneyFormatter.widgetDelta(delta))
                    .font(BrandFont.caption)
                    .foregroundStyle(BrandColor.cyanLight)
                    .padding(.top, 2)
            }

            Text(DropdownFormat.tokens(entry.snapshot.aiTokens) + " tok · \(entry.snapshot.commits) c")
                .font(.system(size: 10, weight: .medium))
                .foregroundStyle(BrandColor.cyanLight.opacity(0.75))
                .padding(.top, 2)

            Spacer(minLength: 4)

            Sparkline(values: entry.snapshot.sparkline, variant: .ai)
                .frame(height: 22)
        }
        .padding(14)
        .containerBackground(for: .widget) {
            BrandSurface { Color.clear }
        }
    }
}
```

> ⚠️ Имена `entry.snapshot.aiCost`, `aiCostDelta`, `aiTokens`, `commits`, `sparkline` — проверить в `Shared/WidgetSnapshot.swift`. Заменить на актуальные.

- [ ] **Step 3: Билд-чек**

```bash
xcodegen generate && xcodebuild -project ai-stats.xcodeproj -scheme StatsWidget -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
```
Expected: `BUILD SUCCEEDED`.

- [ ] **Step 4: Commit**

```bash
git add StatsWidget/Views/StatsWidgetView.swift
git commit -m "feat(widget): Small в новом стиле — BrandSurface + HeroNumber + mini sparkline"
```

---

### Task 20: Medium widget rewrite

**Files:**
- Modify: `StatsWidget/Views/StatsWidgetView.swift` (MediumView)

- [ ] **Step 1: Заменить MediumView**

```swift
struct MediumView: View {
    let entry: StatsTimelineProvider.Entry

    var body: some View {
        HStack(spacing: 0) {
            // Left
            VStack(alignment: .leading, spacing: 0) {
                Crumb(category: .ai, title: "AI", period: entry.configuration.period.localizedTitle)
                HeroNumber(MoneyFormatter.widget(entry.snapshot.aiCost), font: BrandFont.displayL, variant: .pink)
                    .padding(.top, 4)
                if let delta = entry.snapshot.aiCostDelta {
                    Text(MoneyFormatter.widgetDelta(delta) + " " + NSLocalizedString("delta.vs_yesterday", comment: ""))
                        .font(BrandFont.caption)
                        .foregroundStyle(BrandColor.cyanLight)
                        .padding(.top, 2)
                }
                Text(DropdownFormat.tokens(entry.snapshot.aiTokens) + " tok · \(entry.snapshot.commits) c")
                    .font(.system(size: 10))
                    .foregroundStyle(BrandColor.cyanLight.opacity(0.75))
                    .padding(.top, 2)
                Spacer(minLength: 0)
                Sparkline(values: entry.snapshot.sparkline, variant: .ai).frame(height: 22)
            }
            .padding(14)

            Rectangle().fill(SurfaceColor.dividerSubtle).frame(width: 0.5)

            // Right — top models
            VStack(alignment: .leading, spacing: 0) {
                Text("section.top_models")
                    .font(BrandFont.lbl).tracking(1.2).textCase(.uppercase)
                    .foregroundStyle(BrandColor.cyanLight.opacity(0.7))
                    .padding(.bottom, 4)
                ForEach(entry.snapshot.topModels.prefix(4), id: \.model) { m in
                    HStack {
                        Text(m.model).font(.system(size: 11)).lineLimit(1).truncationMode(.middle)
                        Spacer()
                        Text(MoneyFormatter.widget(m.costUsd))
                            .font(.system(size: 11))
                            .monospacedDigit()
                            .foregroundStyle(.white.opacity(0.8))
                    }
                    .padding(.vertical, 2)
                }
                Spacer(minLength: 0)
            }
            .padding(14)
            .frame(maxWidth: .infinity, alignment: .topLeading)
        }
        .containerBackground(for: .widget) { BrandSurface { Color.clear } }
    }
}
```

- [ ] **Step 2: Билд + commit**

```bash
xcodebuild -project ai-stats.xcodeproj -scheme StatsWidget -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
git add StatsWidget/Views/StatsWidgetView.swift
git commit -m "feat(widget): Medium с двумя колонками и divider"
```

---

### Task 21: Large widget rewrite

**Files:**
- Modify: `StatsWidget/Views/LargeView.swift`

- [ ] **Step 1: Прочитать текущую структуру LargeView и переписать**

```swift
struct LargeView: View {
    let entry: StatsTimelineProvider.Entry

    var body: some View {
        VStack(spacing: 0) {
            // Top row — same as Medium top
            HStack(spacing: 0) {
                VStack(alignment: .leading, spacing: 0) {
                    Crumb(category: .ai, title: "AI", period: entry.configuration.period.localizedTitle)
                    HeroNumber(MoneyFormatter.widget(entry.snapshot.aiCost), font: BrandFont.displayXL, variant: .pink)
                        .padding(.top, 4)
                    if let delta = entry.snapshot.aiCostDelta {
                        Text(MoneyFormatter.widgetDelta(delta) + " " + NSLocalizedString("delta.vs_yesterday", comment: ""))
                            .font(BrandFont.caption)
                            .foregroundStyle(BrandColor.cyanLight)
                            .padding(.top, 2)
                    }
                    Text(DropdownFormat.tokens(entry.snapshot.aiTokens) + " tok · \(entry.snapshot.commits) c")
                        .font(.system(size: 10))
                        .foregroundStyle(BrandColor.cyanLight.opacity(0.75))
                        .padding(.top, 2)
                    Spacer(minLength: 0)
                }
                .padding(14)

                Rectangle().fill(SurfaceColor.dividerSubtle).frame(width: 0.5)

                VStack(alignment: .leading, spacing: 0) {
                    Text("section.top_models").font(BrandFont.lbl).tracking(1.2).textCase(.uppercase)
                        .foregroundStyle(BrandColor.cyanLight.opacity(0.7)).padding(.bottom, 4)
                    ForEach(entry.snapshot.topModels.prefix(4), id: \.model) { m in
                        HStack {
                            Text(m.model).font(.system(size: 11)).lineLimit(1).truncationMode(.middle)
                            Spacer()
                            Text(MoneyFormatter.widget(m.costUsd))
                                .font(.system(size: 11)).monospacedDigit()
                                .foregroundStyle(.white.opacity(0.8))
                        }.padding(.vertical, 2)
                    }
                    Spacer(minLength: 0)
                }
                .padding(14)
                .frame(maxWidth: .infinity, alignment: .topLeading)
            }
            .frame(height: 140)

            Rectangle().fill(SurfaceColor.dividerSubtle).frame(height: 0.5)

            // Middle — full sparkline
            VStack(alignment: .leading, spacing: 4) {
                Text("section.trend")
                    .font(.system(size: 9, weight: .medium))
                    .foregroundStyle(.white.opacity(0.5))
                Sparkline(values: entry.snapshot.sparkline, variant: .ai).frame(height: 28)
            }
            .padding(.horizontal, 14)
            .padding(.vertical, 8)

            Rectangle().fill(SurfaceColor.dividerSubtle).frame(height: 0.5)

            // Bottom — friends
            VStack(alignment: .leading, spacing: 0) {
                Text(NSLocalizedString("section.leaderboard", comment: "")
                     + " · " + entry.configuration.period.localizedTitle)
                    .font(BrandFont.lbl).tracking(1.2).textCase(.uppercase)
                    .foregroundStyle(BrandColor.cyanLight.opacity(0.7))
                    .padding(.bottom, 4)

                if entry.snapshot.leaderboard.isEmpty {
                    Text("widget.leaderboard.empty")
                        .font(BrandFont.caption).foregroundStyle(TextColor.muted)
                } else {
                    ForEach(Array(entry.snapshot.leaderboard.prefix(5).enumerated()), id: \.offset) { idx, peer in
                        FriendRow(rank: idx + 1, name: peer.displayName,
                                  valueText: DropdownFormat.tokens(peer.totalTokens),
                                  isMe: peer.isMe)
                    }
                }
                Spacer(minLength: 0)
            }
            .padding(.horizontal, 14).padding(.vertical, 8)
        }
        .containerBackground(for: .widget) { BrandSurface { Color.clear } }
    }
}
```

> ⚠️ Поля `entry.snapshot.leaderboard`, `peer.displayName`, `peer.totalTokens`, `peer.isMe` — проверить в `Shared/WidgetSnapshot.swift`. Если другие — заменить.

- [ ] **Step 2: Билд + commit**

```bash
xcodegen generate && xcodebuild -project ai-stats.xcodeproj -scheme StatsWidget -configuration Debug -derivedDataPath build/ 2>&1 | tail -5
git add StatsWidget/Views/LargeView.swift
git commit -m "feat(widget): Large — top row + sparkline + friends в новом стиле"
```

---

## Phase 7 — Polish & docs

### Task 22: Smoke-build + полный визуальный прогон

**Files:** —

- [ ] **Step 1: Full Release build**

```bash
xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Release -derivedDataPath build/ 2>&1 | tail -10
```
Expected: `BUILD SUCCEEDED`.

- [ ] **Step 2: Run app**

```bash
open build/Build/Products/Release/StatsApp.app
```

- [ ] **Step 3: Прокликать вкладки и состояния**

Проверить визуально:
- Menu bar item — capsule с pink→cyan градиентом.
- Клик → поповер открывается с FloatingIsland внизу.
- Переключение AI / GitHub / Друзья — цвет crumb меняется, контент меняется, sparkline свой по вкладке (Друзья — нет sparkline).
- Переключение Д/Н/М — selection cyan.
- Sync/⚙ — 28×28 квадратики, hover работает.
- На светлом desktop wallpaper поповер сохраняет pink+cyan идентичность (вытащить случайные обои).

Если что-то сломано — TaskCreate отдельная задача с заметкой, что починить.

- [ ] **Step 4: Прогнать тесты**

```bash
xcodebuild test -project ai-stats.xcodeproj -scheme StatsApp -derivedDataPath build/ 2>&1 | tail -15
```
Expected: все тесты passed.

- [ ] **Step 5: Если есть проблемы — fix, иначе следующая задача**

---

### Task 23: README + CHANGELOG

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Обновить README**

Раздел «Что показывает» оставить. В «Требования» подтвердить `macOS 26 Tahoe` (уже сделано в Task 1). Добавить строку в начало (после статуса):

```markdown
**Визуал:** редизайн под Apple Liquid Glass — pink+cyan палитра, floating glass island для переключателей, виджеты на едином языке.
```

- [ ] **Step 2: Добавить CHANGELOG entry**

В верх `CHANGELOG.md` под `## [Unreleased]` (или текущей версией):

```markdown
### Изменено

- Полная переделка визуала под Apple Liquid Glass (B2 Neon Liquid).
  Pink+cyan палитра с внутренним brand-градиентом, не зависящим от обоев.
  Поповер: переключатели категории и периода объединены в floating glass island
  внизу. Виджеты Small/Medium/Large переведены на единый visual language.
  Menu bar item — capsule с pink→cyan градиентом.

### Удалено

- Поддержка macOS 14/15. Минимум — macOS 26 Tahoe (для нативных Liquid Glass API).
```

- [ ] **Step 3: Commit**

```bash
git add README.md CHANGELOG.md
git commit -m "docs: README + CHANGELOG про редизайн под Liquid Glass"
```

---

### Task 24: Финальный merge-ready check

- [ ] **Step 1: Просмотреть diff против main**

```bash
git diff main...HEAD --stat
```

- [ ] **Step 2: Прогнать всё**

```bash
xcodebuild test -project ai-stats.xcodeproj -scheme StatsApp -derivedDataPath build/ 2>&1 | tail -10
xcodebuild -project ai-stats.xcodeproj -scheme StatsApp -configuration Release -derivedDataPath build/ 2>&1 | tail -5
xcodebuild -project ai-stats.xcodeproj -scheme StatsWidget -configuration Release -derivedDataPath build/ 2>&1 | tail -5
```
Expected: тесты pass, оба билда `BUILD SUCCEEDED`.

- [ ] **Step 3: Готов к PR**

```bash
git log --oneline main..HEAD
```
Должно быть ~24 коммита, каждый — атомарный шаг.

PR title: `feat(ui): редизайн под Liquid Glass + Neon Duo (B2)`
PR body — взять из spec'а TL;DR + acceptance criteria.

---

## Спорные / на потом

Эти пункты в этом плане **не делаем** (см. spec § «Не сейчас»):

- Анимации перехода между вкладками.
- Light mode.
- Кастомизация палитры.
- Tooltips для обрезанных имён в виджетах.
- Haptic feedback.

Если в процессе всплывут — оформлять отдельной задачей через `mcp__ccd_session__spawn_task`.

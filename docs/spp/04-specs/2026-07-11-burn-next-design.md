# Спека: Burn — следующая версия (фокус на AI, вкладка «Аналитика»)

Дата: 2026-07-11. Статус: черновик на ревью.

## 1. Цель версии

Burn перестаёт быть «статистикой всего» и становится инструментом про AI-расходы:
показывает, куда уходят токены/деньги/лимиты, и советует, где переключение модели
вернуло бы заметную часть недельного лимита. GitHub и социальный слой (друзья,
лидерборд) выпиливаются как невостребованные.

Три блока изменений:
1. **Выпил**: вкладка GitHub, вкладка «Друзья»/лидерборд и вся обвязка друзей в
   настройках (авторизация через GitHub OAuth остаётся — см. 2.4).
2. **Новая вкладка «Аналитика»**: карточка советника за фиксированные 30 дней
   (статистика + утечки + советы + шпаргалка моделей).
3. **Актуальные прайсы**: gpt-5.6-sol/terra/luna, gpt-5.3-codex-spark и др., чтобы
   виджет и попап показывали реальную стоимость всех живых моделей.

Методика аналитики проверена экспериментом 2026-07-11 (см. память проекта
`model-switch-advisor`): парсинг транскриптов обоих агентов, лестницы моделей,
эвристика + судейские коэффициенты, лимиты Codex из rate_limits.

## 2. Продуктовое поведение

### 2.1 Попап (dropdown)

Floating island внизу переключает **две** вкладки (вместо трёх категорий):

| Вкладка | Содержимое | Периоды |
|---|---|---|
| «Расходы» | текущая AI-вкладка: сумма, дельта, топ моделей, разбивка по источникам, спарклайн | День / Неделя / Месяц (как сейчас) |
| «Аналитика» | карточка советника | фиксированно последние 30 дней, переключателя периода нет |

`DropdownSection` сокращается до `{ .expenses, .analytics }` (сейчас `ai/github/leaderboard`).

### 2.2 Карточка «Аналитика»

Блоки сверху вниз (макет проверен в эксперименте):

1. **Заголовок**: «За последние 30 дней» + диапазон дат (`11.06 — 11.07`).
2. **По источнику** (Codex, Claude Code): потрачено токенов и $, для Codex — средний
   % недельного лимита в неделю (из rate_limits роллаутов); строка «Утекает ≈N% объёма =
   $X/мес ≈ Y п.п. недельного лимита» (для Claude — «% объёма», п.п. лимита недоступны
   локально). **«% объёма» везде считается в долларах**: `Σ exp_saved / Σ cost_usd`
   источника, не в токенах.
3. **«Главные утечки и что делать»**: топ-3 кластера с оценкой $/мес и советом
   (см. 5.5 — типы кластеров и тексты советов).
4. **Шпаргалка «какую модель когда»**: Codex — описания моделей из
   `~/.codex/models_cache.json` (живые, обновляются вместе с Codex): только записи
   с `visibility == "list"`, в порядке следования в файле; файла нет / не парсится —
   Codex-часть шпаргалки скрывается без текста ошибки.
   Claude — статическая таблица в порядке haiku → sonnet → opus → fable
   (haiku — поиск/простые правки; sonnet — основная работа; opus —
   архитектура/глубокое ревью/сложный дебаг; fable — только самые тяжёлые
   long-horizon задачи).

Источник не найден (нет `~/.codex/sessions` или `~/.claude/projects`) — его секция
не показывается; если нет обоих, показывается состояние «нет данных» (см. 7).

Примечание: «Расходы» (ccusage, дневные агрегаты) и «Аналитика» (собственный
парсинг транскриптов) — две разные методики; их суммы за одинаковый период
сходятся приблизительно, а не копейка в копейку. Это ожидаемо, не баг.

### 2.3 Виджеты

- **Small**: без изменений (сумма за период + дельта).
- **Medium**: без изменений (сумма + топ моделей); строки с коммитами удаляются.
- **Large**: становится витриной советника — слева расходы + дельта + токены,
  справа топ моделей, снизу вместо лидерборда строка советника:
  «Утекает ≈$X/мес · <название топ-утечки>» (данные из последнего расчёта карточки).
  Состояния строки: расчёт ещё не выполнялся → строка скрыта; утечек нет →
  «Утечек не видно: модели подобраны по задачам.» Упоминание коммитов
  (`\(entry.commits) c`) удаляется.
- `WidgetSnapshot` теряет поля GitHub/друзей, получает поля советника:
  `advisorComputedAt: Date?`, `leakUsdPerMonth: Double?`, `topLeakTitle: String?`.
  Семантика: `leakUsdPerMonth` = **суммарная** `Σ exp_saved` по обоим источникам
  за 30-дневное окно; `topLeakTitle` = название топ-1 кластера (`nil`, если ни один
  кластер не прошёл фильтр 5.5). Расчёт агрегатов и запись снапшота происходят
  **после каждого успешного ингеста** (старт, часовой тик, открытие вкладки) —
  открывать вкладку «Аналитика» для обновления виджета не требуется.
  Маппинг на состояния строки Large:
  - `advisorComputedAt == nil` (расчёта ещё не было) или режим «мало данных» (§6)
    → строка скрыта;
  - суммарная утечка ≤ $1.00 (сравнение в центах) → «Утечек не видно: модели
    подобраны по задачам.»;
  - иначе → «Утекает ≈$X/мес · {topLeakTitle}»; если `topLeakTitle == nil` —
    без второй части, просто «Утекает ≈$X/мес».

### 2.4 Настройки

- Остаются вкладки: **Общие** (автозапуск и т.п.), **Аккаунт** (авторизация — нужна
  для синка своей статистики между устройствами, `SnapshotSyncer` не меняется).
- **Важно:** авторизация построена на GitHub OAuth (`AuthService: GitHubSignInService`),
  поэтому `Config.githubToken/githubLogin`, `AuthDTO.githubToken/githubLogin` и
  `GithubAvatar` (аватар аккаунта) **остаются** — это часть аккаунта, а не вкладки GitHub.
  Токен перестаёт использоваться только для статистики коммитов.
- Удаляются: вкладка **Друзья** (`FriendsTabView/-ViewModel`, `FriendCode` UI),
  вкладка **Заблокированные** (`BlockedTabView/-ViewModel`). Поле `friendCode` в
  `AuthExchangeResponse` остаётся в DTO (серверный контракт), но нигде не показывается.
- Экспорт/импорт БД остаются.

### 2.5 Menu bar capsule

Без изменений (сумма за день).

## 3. Выпил по модулям

| Удаляется | Замечания |
|---|---|
| `StatsApp/Sources/GitHubFetcher.swift`, `GitHubDTO.swift` | вместе с веткой `.github(GitHubFetchPayload)` в `FetcherProtocol` |
| `DropdownSection.github`, `.leaderboard` + их секции в `DropdownView`/`DropdownSections` | |
| `Shared/Design/FriendRow.swift`; в `CrumbCategory` — кейсы `.github` и `.friends` (остаётся `.ai`) | Sparkline-категория github тоже |
| `FriendsTabView(-ViewModel)`, `BlockedTabView(-ViewModel)`, `FriendCode` | |
| Лидерборд-эндпоинты в `AiuseAPIClient` (friends/leaderboard/blocked) | snapshot-эндпоинты остаются |
| `StatsApp/Sync/FriendsPullSyncer.swift`, `LeaderboardPullSyncer.swift` | `SyncCoordinator` остаётся **с правками**: из него удаляются вызовы pull-синкеров |
| Аватарная обвязка лидерборда в `WidgetSnapshotIO` (`writeAvatar/readAvatar/pruneAvatars`, каталог `avatars/`) | аватар самого аккаунта в настройках не задет |
| Поля GitHub/друзей в `WidgetSnapshot` (`commits`, `uniqueRepos`, `githubEnabled`, `leaderboard`, `myFriendCode` и пр.) | см. 7 — версионирование снапшота |
| Строки локализации и скриншоты README по GitHub/друзьям | README обновить |

Остаётся: `AuthService` (GitHub OAuth — базис аккаунта), `GithubAvatar` (аватар в
Аккаунте), GitHub-ключи в `Config`/`AuthDTO`, `KeychainStore`, `AiuseAPIClient`
(auth + snapshots), `SnapshotSyncer`, `SyncCoordinator` (после правок), ccusage-пайплайн
для «Расходов».

## 4. Прайсы (PricingTable.swift)

Добавить точные записи (USD за 1M токенов):

| Модель | input | output | cache read | Статус ставки |
|---|---|---|---|---|
| `gpt-5.6-sol` | 5.00 | 30.00 | 0.50 | допущение: позиционируется как frontier ⇒ ставка gpt-5.5 |
| `gpt-5.6-terra` | 2.50 | 15.00 | 0.25 | допущение: «balanced/everyday» ⇒ ставка gpt-5.4 |
| `gpt-5.6-luna` | 0.75 | 4.50 | 0.075 | допущение: «fast and affordable» ⇒ ставка gpt-5.4-mini |
| `gpt-5.3-codex-spark` | 0.75 | 4.50 | 0.075 | допущение: subscription-only (`supported_in_api: false`), публичного прайса нет ⇒ ставка mini |

- Все четыре — с комментарием «ставка-допущение, публичного прайса нет; сверить при
  появлении» и предупреждением в `AppLogger.pricing` один раз за запуск.
- Family-fallback расширить: префикс `gpt-5.6-` без точного совпадения → ставка gpt-5.5
  (консервативно сверху) с warning-логом — чтобы новая gpt-5.6-* не считалась нулём.
- Обоснование: нулевая ставка занижает статистику на сотни $ в месяц (эксперимент:
  gpt-5.6-sol ≈ $437/нед считался нулём); завышенное допущение честнее нуля и явно помечено.

## 5. Подсистема «Аналитика»

### 5.1 Ингестор транскриптов

Новый модуль `StatsApp/Analytics/` (ингестор + расчёт + модели карточки).

- Источники: `~/.claude/projects/**/*.jsonl` (Claude Code), `~/.codex/sessions/**/*.jsonl` (Codex).
- Запуск: при старте приложения; далее — внутри существующего тика
  `SyncCoordinator` с гейтом «не чаще раза в час» (отдельный таймер не заводится);
  дополнительно при открытии вкладки «Аналитика», если последний ингест старше 15 минут.
- Инкрементальность — **на уровне файлов, не оффсетов**: таблица
  `analytics_ingest_state(path, mtime, size)`; файл перечитывается **целиком**, только
  если mtime/size изменились с прошлого ингеста. Дочитывание с байтового оффсета
  запрещено: у Codex токены — дельты кумулятивного счётчика внутри файла (resume с
  середины теряет базу), а записи хвоста могут принадлежать ходу, начатому до оффсета.
  Ходы пишутся политикой **upsert** (`INSERT OR REPLACE` по UNIQUE-ключу): ход,
  заингещённый усечённым во время живой сессии, дополняется при следующем тике.
  Стоимость приемлема: за тик меняются только файлы активных сессий (единицы),
  полный месяц истории парсится < 1 мин (замер прототипа на Python; Swift быстрее).
- **Окно и ретеншн**: ингестор окном НЕ режет — пишет всю доступную на диске историю
  (первый запуск = бэкфилл всего, включая Codex-сессии старше 30 дней); прунинга
  `analytics_turns` в этой версии нет — БД и есть постоянная история. Окно
  «последние 30 дней» накладывается **на этапе расчёта карточки** (запросом),
  граница — `now − 30 дней` в момент расчёта. Это санкционированное расхождение
  с прототипом (тот для скорости режет окно на парсинге): сверка §9.3 корректна,
  потому что оба считают карточку по одинаковому окну от одного `now`.
- **Таймзона**: `day` и границы окна — локальная таймзона устройства.
  В прототипе захардкожена Europe/Moscow — тоже санкционированное расхождение;
  сверку §9.3 гонять с совпадающей таймзоной.
- Причина срочности: retention транскриптов Claude Code ≈ 30 дней; своя БД делает
  историю постоянной.

Правила парсинга: **нормативная референс-реализация — `scripts/analytics-prototype/experiment2.py`
целиком** (при расхождении текста спеки и прототипа правильным считается прототип,
а спека правится). Ключевые правила:
- Claude Code: ход = user-запись с реальным промптом (не tool_result, не isMeta,
  не sidechain) + все assistant-записи до следующего промпта; usage дедупить по
  `message.id` (одно сообщение пишется несколькими строками), брать последнее
  вхождение; `<synthetic>`-модель пропускать; cache-write разделять на 5m/1h по
  `usage.cache_creation`; тул-коллы — `tool_use`-блоки assistant-записей основной
  цепочки (не-sidechain), правки (`n_edits`) — тулы `Edit/Write/MultiEdit/NotebookEdit`.
- Codex: ход = `turn_context` (model, effort); токены — дельты кумулятивного
  `token_count.total_token_usage` (`cached_input` — подмножество input); тул-коллы —
  `response_item` типов `custom_tool_call|function_call|local_shell_call`, правки
  (`n_edits`) — тул с `patch` в имени; лимиты — `rate_limits.used_percent`
  (primary 5ч / secondary неделя) при `limit_id == "codex"`.

### 5.2 Схема БД (GRDB, существующая база)

```sql
analytics_turns (
  id INTEGER PK, source TEXT, ts TEXT, day TEXT, session TEXT, project TEXT,
  model TEXT, effort TEXT, origin TEXT,           -- human | auto, см. правило ниже
  prompt_head TEXT, prompt_chars INT,             -- первые 300 символов, только локально
  n_requests INT, n_tool_calls INT, n_edits INT,
  input_tokens INT, cache_read INT, cache_create_5m INT, cache_create_1h INT,
  output_tokens INT, cost_usd REAL,
  heur_tier INT, cf_model TEXT, cf_usd REAL, exp_saved_usd REAL,
  UNIQUE(source, session, ts)
)
analytics_ingest_state (path TEXT PK, mtime REAL, size INT)
analytics_rate_limits (
  path TEXT, ts TEXT, window TEXT, used_percent REAL,   -- codex 5h/week
  UNIQUE(path, ts, window)                              -- INSERT OR REPLACE;
)                                                       -- перечитывание файла не плодит дублей
```

Правило `origin`: промпт начинается с `<command-` → `human` (слэш-команда);
начинается с любого другого `<` (в т.ч. `<task-notification`) → `auto`; иначе `human`.
Использование: `auto`-ходы участвуют в суммах и вердиктах наравне со всеми, но
кластеризуются в спецкластер «Фоновые уведомления» (см. 5.5) и не попадают в
кластеры «болталки».

**Приватность (жёсткое правило):** `prompt_head` никогда не покидает устройство —
в `SnapshotSyncer`/aiuse-api уходят только дневные агрегаты, как сейчас. В экспорт БД
(`DatabaseExporter`) prompt_head входит (локальный файл пользователя) — допустимо.

### 5.3 Вердикты: лестницы и коэффициенты

- Классификация моделей по ступеням — **все совпадения по префиксу** имени модели
  (как family-fallback прайсов; `gpt-5.5` матчит и гипотетический `gpt-5.5-codex`):
  - **T0 (frontier)**: `claude-fable-`, `claude-mythos-`, `claude-opus-`,
    `gpt-5.5`, `gpt-5.6-sol`;
  - **средняя**: `claude-sonnet-`, `gpt-5.4` (кроме `gpt-5.4-mini`), `gpt-5.6-terra`;
  - **маленькая**: `claude-haiku-`, `gpt-5.4-mini`, `gpt-5.3-codex-spark`, `gpt-5.6-luna`.
  Порядок проверки префиксов — от более специфичного к менее (mini раньше 5.4).
  Вердикт «мог быть дешевле» ставится **только T0-ходам**; ходы на средних/маленьких
  моделях считаются уже оптимальными (в v1 даунгрейд среднего→маленькое не оцениваем).
- Целевые модели даунгрейда (counterfactual-ставки): claude → `sonnet-4-6` (tier 1),
  `haiku-4-5` (tier 2); codex → `gpt-5.4` (tier 1), `gpt-5.4-mini` (tier 2; в тексте
  советов упоминаются также luna/spark как равнозначные варианты — отдельной ставки нет).
- Эвристика (только T0-ходы): `n_edits==0 && n_tools==0 && out<800 && prompt<1200` → tier 2;
  `n_edits==0 && n_tools<=4 && out<4000` → tier 1; иначе tier 0.
- Ожидаемая экономия хода — одна формула (псевдокод):

  ```
  if модель не T0:      exp_saved = 0
  else if tier == 0:    exp_saved = cost_usd × judgeRatioT0(source)   // судья находит
                                                                       // даунгрейды и внутри «по делу»
  else /* tier 1|2 */:  exp_saved = max(0, cost_usd − cf_usd)          // полный counterfactual
  ```

- Судейские коэффициенты v1 — **только для страты tier 0**: claude `judgeRatioT0 = 0.057`,
  codex `judgeRatioT0 = 0.072` (откалиброваны LLM-разметкой выборки n=79, 2026-07-11).
  Хранятся константами с датой калибровки; живой LLM-судья — вне скоупа (нет API-ключа).
- Обоснование: формула повторяет прототип `experiment3.py`; коэффициенты переносят
  «знание судьи» без затрат и сети; расчёт детерминированный и пересчитываемый.

### 5.4 Лимиты Codex

- Недельный расход в п.п.: по эпохам ряда secondary.used_percent — сброс эпохи при
  падении >30 п.п. от максимума; вклад эпохи = max − первое наблюдение. Сумма
  положительных дельт запрещена (мусор из-за параллельных сессий — проверено).
- «В среднем %/нед» = сумма эпох за 30 дней / (30/7).
- Строка «≈Y п.п. недельного лимита» = доля утечки от объёма × средненедельный %.
- Для Claude Code показываем только «% объёма»; интеграция usage-эндпоинта — вне скоупа.

### 5.5 Кластеризация утечек и советы

- Ключ кластера: **источник + нормализованное начало промпта** (lowercase, первые
  60 символов) — кластеризация ведётся в разрезе источника, кластеры Codex и Claude
  не смешиваются. Спецключи (согласованы с правилом `origin` из 5.2):
  `<command-…` → «Слэш-команды»; **любой другой промпт, начинающийся с `<`**
  (origin=auto, включая `<task-notification…`) → «Фоновые уведомления».
  Тексты советов подставляются по источнику кластера.
- Кластер попадает в топ при ≥3 ходах и оценке **строго больше $1.00/мес**
  (сравнение в целых центах: `exp_saved_cents > 100`). Сортировка: по exp_saved DESC,
  при равенстве — по числу ходов DESC, затем по ключу лексикографически. Топ-3
  (общий по обоим источникам).
- Совет по типу кластера (точные тексты):
  - роль пайплайна (промпт начинается с «Ты — »): «Зафиксируй модель этой роли в
    конфиге пайплайна: {sonnet-4-6 | gpt-5.4}»;
  - фоновые уведомления: «Фоновые уведомления обрабатывай дешёвой моделью»;
  - слэш-команды: «Слэш-команды наследуют модель сессии — тяжёлые команды запускай
    в сессии на средней модели»;
  - остальное: «Короткие вопросы — в лёгкий чат на {haiku | luna/spark}».
- Подпись кластера: «N ходов на {доминирующая по экономии модель} · ≈$X/мес».

## 6. Доменный чеклист

- **Деньги**: карточка и виджеты — целые доллары через существующий
  `MoneyFormatter.widget` (`$1 603`, NBSP-разделитель тысяч, .rounded(), не banker's);
  суммы <$1 — «<$1»; попап «Расходы» — как сейчас с центами. Экономия в месяц —
  целые доллары.
- **Проценты**: % объёма — целые («≈9%»); п.п. недельного лимита — один знак («5.9 п.п.»);
  средненедельный лимит — целые («≈64%/нед»).
- **Токены**: в карточке «Аналитика» — новый форматтер `AnalyticsFormat.tokens`:
  ≥1 млрд — «1.92 млрд ток» (два знака); ≥1 млн — «412 млн ток» (целые);
  ≥1 тыс — «512 тыс ток» (целые); меньше — «312 ток». Существующий
  `DropdownFormat.tokens` («%.1fM»/«%.0fk») **не меняется** — вкладка «Расходы» и
  Small/Medium виджеты выглядят как сейчас.
- **Списки**: утечки — сортировка/tie-break из 5.5; шпаргалка Codex — в порядке
  следования записей в `models_cache.json` (фильтр `visibility == "list"`),
  Claude — фиксированный порядок haiku→sonnet→opus→fable.
- **Пользовательский ввод**: в новой фиче полей ввода нет — ось не применяется.
- **Float-инвариант**: cost хранится в Double USD; сравнения порогов — в целых центах
  (`Int(round(usd*100))`); суммирование — по Double, отображение только через форматтеры.
- **Тексты состояний** (дословно):
  - нет данных вообще: «Пока нечего анализировать. Burn читает историю Claude Code
    и Codex локально — поработай с агентами несколько дней, карточка соберётся сама.»
  - данных мало (<50 ходов за 30 дней **суммарно по обоим источникам**): «Мало данных
    для выводов — карточка появится, когда наберётся история.» В этом режиме поля
    советника в снапшоте остаются `nil` → строка Large-виджета скрыта (не «утечек нет»).
  - утечек нет (суммарная `Σ exp_saved` ≤ $1.00, сравнение в центах — то же условие,
    что у Large-виджета): «Утечек не видно: модели подобраны по задачам.»
  - лимит Codex недоступен (нет rate_limits): строка лимита не показывается, без текста.
  - ингест идёт первый раз: спиннер + «Разбираю историю за 30 дней…»

## 7. Миграции и совместимость

- GRDB-миграция: создание трёх analytics-таблиц; удаление данных GitHub/друзей
  не требуется (таблицы остаются мёртвыми — чистка в следующем мажоре, чтобы
  экспорт/импорт старых БД не ломался; `DatabaseValidator` не должен требовать
  удалённые сущности).
- `Config`: ключи `github_token/github_login` остаются (авторизация); использование
  токена GitHub-статистикой исчезает вместе с `GitHubFetcher`.
- Снапшот-синк и серверная схема aiuse-api не меняются.
- Виджет: `WidgetSnapshot` меняет схему. Механизма версий у снапшота сейчас нет —
  вводится поле `schemaVersion: Int` (= 2). Если `snapshot.json` на диске не
  декодируется или `schemaVersion` отсутствует/меньше 2, виджет показывает
  placeholder «Открой Burn, чтобы обновить данные» до первой записи нового
  снапшота приложением.

## 8. Вне скоупа версии

- Живой LLM-судья (Haiku) и пересчёт коэффициентов.
- Живой used_percent для Claude Code (OAuth usage-эндпоинт).
- Уведомления/алерты о приближении к лимиту.
- Переименование bundle ID / путей под Burn (запланировано к v1.0 отдельно).

## 9. Критерии приёмки

1. В попапе две вкладки; «Расходы» ведут себя как текущая AI-вкладка; «Аналитика»
   показывает карточку за 30 дней без переключателя периода.
2. Ходы за 30 дней из обоих источников попадают в `analytics_turns`; повторный ингест
   не дублирует, а усечённые ходы живых сессий дополняет (upsert по UNIQUE-ключу).
3. Суммы карточки (±5%) сходятся с прогоном эталонного прототипа
   `scripts/analytics-prototype/experiment3.py` на тех же данных и том же окне
   (прототип лежит в репо и является исполняемой референс-реализацией методики).
   Сверка всегда идёт против **свежего прогона прототипа** на текущих данных —
   retention транскриптов ~30 дней, старые окна невоспроизводимы. Контрольные
   значения прогона 2026-07-11 ниже — иллюстрация порядка величин, не эталон
   (окно 11.06—11.07, ставки-допущения §4, до добавления ставок terra/luna/spark):
   Codex — 1.92 млрд ток, $1 816, утечка $168/мес (9%); Claude Code — 4.50 млрд ток,
   $3 844, утечка $410/мес (11%); топ-утечка — фоновые task-notification на
   opus-4-8 ≈ $92/мес.
4. gpt-5.6-sol/terra/luna и spark имеют ненулевую стоимость в топе моделей.
5. GitHub и друзья отсутствуют в UI, настройках и сетевых вызовах (кроме auth/snapshots);
   авторизация и синк работают.
6. Large-виджет показывает строку советника; Small/Medium — без упоминаний коммитов.
7. Тесты: парсер обоих форматов (фикстуры — анонимизированные срезы реальных
   транскриптов: структура записей настоящая, тексты промптов заменены на
   плейсхолдеры — реальные промпты в репо не попадают), эвристика тиров,
   эпохи лимита, кластеризация, миграция БД — зелёные.

---
Приложение: методика и цифры эксперимента — память проекта `model-switch-advisor`,
прототипы `experiment2.py`/`experiment3.py`/`judge_load.py` (scratchpad сессии 2026-07-11).
